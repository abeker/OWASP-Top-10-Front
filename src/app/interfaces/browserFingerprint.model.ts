export interface BrowserFingerprint {
    fingerprint: string;
    browserName: string;
    browserVersion: string;
    os: string;
    osVersion: string;
    cpu: string;
    screenPrint: string;
    plugins: string;
    language: string;
    timeZone: string;
}
